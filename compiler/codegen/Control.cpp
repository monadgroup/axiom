#include "Control.h"

#include <cassert>

using namespace MaximCodegen;

Control::Control(ControlDeclaration *declaration) {

}

Value* Control::getProperty(std::string name) {
    // todo
    assert(false);
    throw;
}

bool Control::setProperty(std::string name, std::unique_ptr<Value> value) {
    // todo
    assert(false);
    throw;
}

void Control::setMode(Mode mode) {
    // todo
    assert(false);
    throw;
}
